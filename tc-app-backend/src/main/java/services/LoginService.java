package services;

public class LoginService {

	/*
	 * private final String USERS_COLLECTION = "user";
	 * 
	 * public String createNewUser(User user) { DbCommunication db = new
	 * DbCommunication(); Document doc = new Document("username",
	 * user.getUserName()).append("password", user.getPassword()); Document
	 * document = db.findOne(USERS_COLLECTION, doc).first(); if (document ==
	 * null) { db.addToCollection(USERS_COLLECTION, doc); } db.closeDb(); return
	 * findUser(user); }
	 * 
	 * public String findUser(User user) { DbCommunication db = new
	 * DbCommunication(); Document doc = new Document("username",
	 * user.getUserName()).append("password", user.getPassword()); String
	 * docFound = db.findOne(USERS_COLLECTION, doc).first().toJson();
	 * db.closeDb(); return docFound; }
	 */
}
