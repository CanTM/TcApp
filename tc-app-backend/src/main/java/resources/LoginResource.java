package resources;

import javax.ws.rs.Path;

@Path("login")
public class LoginResource {

	/*
	 * @POST
	 * 
	 * @Path("newUser")
	 * 
	 * @Consumes("application/x-www-form-urlencoded")
	 * 
	 * @Produces("text/plain") public String
	 * createNewUser(@FormParam("userName") String
	 * userName, @FormParam("password") String password) { LoginService login =
	 * new LoginService(); return login.createNewUser(new User(userName,
	 * password)); }
	 * 
	 * @GET
	 * 
	 * @Path("autenticate")
	 * 
	 * @Produces("text/plain") public String autenticate(@QueryParam("userName")
	 * String userName, @QueryParam("password") String password) { LoginService
	 * login = new LoginService(); return login.findUser(new User(userName,
	 * password)); }
	 */
}
